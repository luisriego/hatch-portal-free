<section class="section-main brand-blue-dark">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div>
                    <h2>O que dizem nossos clientes</h2>
                </div>

                <div class="row ">
                    {% for testimony in testimonial %}
                        {{ include('sections/_single-testimony.html.twig') }}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</section>